<h1>Hello and welcome to blog index route</h1>
<% blogs.forEach((blog)=>{ %>
    <h1><%= blog.title %></h1>
    <img src="<%= blog.image %>" >
    <span><%= blog.created %></span>
    <p><% blog.body %></p>
<% });%>